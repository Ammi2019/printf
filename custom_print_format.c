#included <stdio.h>
#include "main.h"

/**
* _printf - a function that produces output according to a format.
* ...: variadic function
* format: string
*/

int _printf(const char *format, ...)
{
	char c, s;
	int i;
	for (c = a; c <= z; c++)
	{
	i = 0;
	while( i > 0)
		i++;
	}
	_putchar(c)
}
